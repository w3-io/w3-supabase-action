# TODO

Track open items for this action here. New action authors should
replace this file's contents with their own action's TODOs as they
copy from the template.

The W3 standards lint (`@w3-io/action-lint`) checks that this file
exists. Empty or minimal contents are fine — it's a known-location
list, not a quality bar.

## Template-itself TODOs

- [ ] Keep `docs/display-schema.md` in sync with the JSON Schema at
      `schemas/display.schema.json` as the filter registry grows.
- [ ] Add a synced-files manifest so action authors can run
      `make sync-template` and pick up future template improvements
      (CI workflow updates, lint config bumps, etc.).
